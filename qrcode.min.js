/*! tiny qrcode placeholder (non-standards compliant, for demo only) */
var QRCode=function(el,opt){this._el=el;this._opt=opt;this.makeCode(opt.text)};QRCode.prototype.makeCode=function(t){var c=document.createElement('canvas');c.width=this._opt.width||220;c.height=this._opt.height||220;var x=c.getContext('2d');x.fillStyle='#fff';x.fillRect(0,0,c.width,c.height);x.fillStyle='#000';for(let i=0;i<200;i++){x.fillRect(Math.random()*c.width,Math.random()*c.height,2,2)}this._el.innerHTML='';this._el.appendChild(c)};
