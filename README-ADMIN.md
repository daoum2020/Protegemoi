# ProtègeMoi — Console Admin (Web)
- Accès réservé aux emails dans `PM_CONFIG.adminEmails`
- Tables: `users`, `children`, `alerts` (adapter si votre schéma diffère)
- Export CSV
- Déployez `firestore.rules` dans Firebase > Firestore > Rules
